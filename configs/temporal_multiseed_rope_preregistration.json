{
  "status": "pre-registered before any multi-seed or RoPE result is observed",
  "date": "2026-08-13",
  "purpose": "audit seed sensitivity and test temporal RoPE under a fixed training budget",
  "architectures": {
    "mlp": {"hidden_dim": 64, "layers": 1},
    "rnn": {"hidden_dim": 48, "layers": 1},
    "lstm": {"hidden_dim": 24, "layers": 1},
    "gru": {"hidden_dim": 28, "layers": 1},
    "transformer": {"hidden_dim": 16, "layers": 2, "heads": 4},
    "transformer_rope": {"hidden_dim": 16, "layers": 2, "heads": 4}
  },
  "training": {
    "split": "MISATO-100 train only (80 independent complexes)",
    "objective": "standardized one-step invariant-feature MSE with teacher forcing",
    "epochs": 200,
    "checkpoint_policy": "final epoch for every architecture and seed; no validation epoch selection",
    "batch_size": 16,
    "optimizer": "Adam",
    "learning_rate": 0.001,
    "seeds": [0, 42, 123],
    "parameter_policy": "all models below 10k trainable parameters; counts reported"
  },
  "evaluation_interval_epochs": 20,
  "controls": {
    "static": "repeat structural state and set the four future step-magnitude features to physical zero before standardization",
    "order_1": "MLP",
    "position_ablation": "absolute sinusoidal Transformer versus otherwise comparable RoPE Transformer"
  },
  "promotion": {
    "primary": "mean T1/T2/T3-weighted validation rollout RMSE across seeds",
    "seed_consistency": "beat paired MLP in at least 2 of 3 seeds",
    "memory_signal": "beat mean MLP weighted RMSE and mean Static in at least two scenarios",
    "long_history_signal": "improve mean T2 or T3 RMSE over MLP by at least 5%",
    "robustness": "all reported values finite",
    "test_access": "prohibited"
  },
  "evidence_boundary": "12D invariant proxy only; cannot establish atomic Geo/Phys/Dyn/Stab or official score"
}
